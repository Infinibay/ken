<work-summary>
Internal record of work completed in the previous task (not shown to the user). Use it for continuity; reground specifics with tools before relying on them.

- **Files changed:** No product files were modified and no commits were made. Research notes were added, but their filenames are not recorded. Git cleanliness was not verified.
- **Investigation completed:** Read `src/ken/_paths.py:51–66`: `resolve_project_path` applies `expanduser` and `resolve`, then checks containment with `relative_to(root)`; escapes raise `ValueError`. Traced `server.py`: `run` establishes the project root (91–105); `ken_tool` registers without exception wrapping (192–196); `ken_read` validates before index access (1000), through `_project_relative_path` (325–330), then routes `source` through `_impl_ken_file_snippets` (474–491).
- **Important findings:** In `search.py`, `file_snippets` requires an index entry (408–482). An internal nonexistent path passes containment validation but returns `file not indexed` without an entry. This is a nested `source.ok:false`; outer `ken_read.ok` remains true. An indexed, deleted file can produce `code:''` with `ok:true` because `_read_line_range` catches `OSError` (789–796). `_normalize_index_path` is not a security boundary. No second containment check or atomic protection against symlink changes exists between validation and reading.
- **Problems / unfinished verification:** Delegated matrix and pytest execution to Lucía under ticket `t_c6d175f19f74`; tooling rejected execution with `discovery_suppressed`. Ticket was closed as canceled; task ended blocked. Do not repeat blocked attempts or claim tests passed.
- **Follow-up:** Experimental verification remains outstanding; use repository-local fixtures without product edits or network access if execution becomes available. The Spanish final response summarized static findings and the execution block, but did not actually include the detailed references or reproducible script mentioned as deliverables.
</work-summary>