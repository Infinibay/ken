Voy a consultar la nota señalada y el contrato en MCP y `ken tools`, sin modificar archivos ni memorias.
El soporte ya existe. Registraría la nueva función tipada con `@ken_tool` en [server.py](/private/tmp/ken-coding-memory-4n2epo1i/mcp-baseline/src/ken/mcp/server.py:192): publica en MCP y registra la definición que consume `ken tools`. Mantendría la lógica de negocio fuera del adaptador.

El contrato actual genera `{"type":"array","items":{"type":"object"}}` mediante `_annotation_to_schema` y `_items_schema`. En [cli.py](/private/tmp/ken-coding-memory-4n2epo1i/mcp-baseline/src/ken/cli.py:1757), `_tool_py_type` selecciona `_tool_json_object`, que decodifica cada argumento y rechaza valores que no sean objetos. MCP recibe una lista JSON; CLI recibe objetos separados: `--facts '{"x":1}' '{"x":2}'`, no un único array JSON. Una lista requerida es posicional y exige al menos un elemento; conviene definir explícitamente si debe admitir `[]`.

La nota `mcp-object-cli-parameters` documenta los bugs anteriores: esquema sin objetos en `items` y argumentos recibidos como cadenas. Recomiendo ampliar [estos tests](/private/tmp/ken-coding-memory-4n2epo1i/mcp-baseline/tests/test_reasoning_memory.py:182): verificar ambos esquemas y ejecutar **`ken.cli.main`** con varios objetos, comparando argumentos y resultado con MCP; incluir JSON inválido, valores no objeto y lista vacía. Invocar únicamente las funciones envoltorio no comprueba la conversión CLI.