language "kql/2"; module investigation;
query missing_close {
  callable $target {name:"_conn";}
  callable $owner {
    call $site {target:$target;}
    not exists {call $cleanup {name:/^(close|closing)$/;}}
  }
  select $owner;
}